## HPBD Lê Ngọc Hiếu

Thiệp sinh nhật online đơn giản, dùng HTML/CSS/JS thuần và một chút Bootstrap.

### ✨ Điểm nổi bật
- Ảnh nền và tuyết rơi tạo cảm giác mùa đông.
- Modal hiển thị thiệp với lời chúc và nhạc nền `.mp3`.
- Hoạt hình nhỏ cho tấm thiệp (jQuery animate).

### 🚀 Chạy trên máy
1. Clone hoặc tải code về máy.
2. Mở terminal tại thư mục dự án và chạy một trong các cách:
	- `python -m http.server 8000`
	- `npx serve .`
	- Hoặc dùng Live Server trong VS Code.
3. Truy cập `http://localhost:8000` và nhấp vào tấm thiệp để mở modal + nhạc.

### ☁️ Deploy
- Repo sẵn sàng đưa lên Vercel (Project name gợi ý: `HPBD-LNH`).
- Thiết lập framework = `Other`, để trống lệnh build, Output dir = `.`.

### 📂 Cấu trúc chính
```
index.html   # Trang chính
css/         # Bootstrap + custom style
js/          # jQuery, Bootstrap, hiệu ứng tuyết, script chính
img/         # Ảnh thiệp
ms/          # File âm thanh chúc mừng
```

Chúc bạn có thêm những món quà nhỏ xinh như dự án này! 
